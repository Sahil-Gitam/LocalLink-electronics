apiVersion: apps/v1
kind: Deployment
metadata:
  name: localink-frontend-canary
  namespace: localink
  labels:
    app: localink
    component: frontend
    version: canary
spec:
  replicas: 1  # Start with 1 replica, scale based on canary percentage
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: localink
      component: frontend
      version: canary
  template:
    metadata:
      labels:
        app: localink
        component: frontend
        version: canary
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: frontend
        image: <DOCKER_REGISTRY>/localink-frontend:canary
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: localink-config
              key: NODE_ENV
        - name: REACT_APP_API_URL
          value: "http://localink-backend:5000"
        - name: VERSION
          value: "canary"
        - name: CANARY_DEPLOYMENT
          value: "true"
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
