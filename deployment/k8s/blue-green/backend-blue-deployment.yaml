apiVersion: apps/v1
kind: Deployment
metadata:
  name: localink-backend-blue
  namespace: localink
  labels:
    app: localink
    component: backend
    version: blue
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: localink
      component: backend
      version: blue
  template:
    metadata:
      labels:
        app: localink
        component: backend
        version: blue
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: backend
        image: <DOCKER_REGISTRY>/localink-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: localink-config
              key: NODE_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: localink-config
              key: PORT
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: localink-secrets
              key: MONGO_URI
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: localink-secrets
              key: JWT_SECRET
        - name: PINECONE_API_KEY
          valueFrom:
            secretKeyRef:
              name: localink-secrets
              key: PINECONE_API_KEY
        - name: PINECONE_HOST
          valueFrom:
            secretKeyRef:
              name: localink-secrets
              key: PINECONE_HOST
        - name: PINECONE_INDEX
          valueFrom:
            configMapKeyRef:
              name: localink-config
              key: PINECONE_INDEX
        - name: GOOGLE_AI_API_KEY
          valueFrom:
            secretKeyRef:
              name: localink-secrets
              key: GOOGLE_AI_API_KEY
        - name: VERSION
          value: "blue"
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
      restartPolicy: Always
      terminationGracePeriodSeconds: 60
